// Importa el paquete Material de Flutter.
import 'package:flutter/material.dart';

// Define una constante de color para ser utilizada como color de fondo en la aplicación.
// La constante 'backgroundcolor' usa un valor hexadecimal específico para el color.
const backgroundcolor = Color.fromARGB(255, 251, 251, 251);

// Crea una variable 'theme' que contiene la configuración del tema global de la aplicación.
// 'ThemeData' es una clase que almacena las configuraciones de tema para una aplicación de Material Design.
final theme = ThemeData(
  fontFamily:
      'Monserrat', // Define 'Monserrat' como la fuente predeterminada para todo el texto en la aplicación.
);
